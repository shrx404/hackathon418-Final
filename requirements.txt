# Can use this if requirements.txt doesn't work.
# Python 3.11 or 3.12 recommended (3.10 works but is not officially supported)
# ---------------------------------------------------------------------------
# Core ML stack
terratorch>=1.2.4         # TerraMind backbone + terratorch tasks
torch>=2.0.0              # PyTorch (terratorch installs compatible version)
torchgeo>=0.6.0           # SENTINEL2_ALL_SOFTCON added in 0.6.0 (required by terratorch 1.2.6)
numpy>=1.24.0

# Geospatial I/O (for .tif loading in infer.py)
rasterio>=1.3.0

# FastAPI service
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-dotenv==1.0.1
python-multipart>=0.0.9

# Image encoding (PNG mask generation in API)
Pillow>=10.0.0


#SERVER IMPORTS
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
numpy>=1.24.0
Pillow>=10.0.0
tifffile>=2023.1.1

# HUGGING FACE
huggingface_hub